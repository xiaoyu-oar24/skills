---
name: "code-search-discipline"
description: "代码搜索纪律：搜索前先确认 codegraph MCP 对当前项目是否可用，复用检查结果；可用且适合任务时优先 codegraph，不可用或能力不覆盖时降级到受限搜索。强制 grep 限定模块目录或文件类型，绕开依赖、构建产物与静态资源，避免全仓扫描和重复读取。TRIGGER when: 需要搜索、查找、定位代码、符号、函数、字符串、报错文案所在文件，理解源码结构或调用关系，或即将调用 grep 和文件搜索工具。SKIP: 仅读取已知路径的单个文件（用 Read）；纯文档瘦身与检索治理（用 lean-docs）。"
version: "1.2.0"
author: "xiaoyu"
---

# 代码搜索纪律 (code-search-discipline)

> **核心思想**：先确认 codegraph 可用性，再按能力选择工具。检查结果按项目复用，优先用一次有界的 codegraph 查询获取所需源码与关系；降级搜索必须限定范围，已有上下文不重复读取。

## 🎯 触发条件

- **TRIGGER when**（任一命中即激活，本技能是**前置约束**，先于任何搜索工具调用生效）:
  - 任务需要搜索、查找、定位代码：符号、函数、变量、字符串、报错文案、配置值出现在哪个文件哪一行
  - 需要理解大型源码：某个功能如何实现、某个函数被谁调用、改动会影响哪些模块
  - 即将调用任何内容搜索工具（grep）或文件枚举工具（glob / codegraph 系列）
- **SKIP**:
  - 已知文件路径，仅需读取单个文件内容 → 直接用 Read，无需搜索
  - 纯文档瘦身、文档检索治理、降低文档 Token 消耗 → 用 `lean-docs`
  - 纯日志检索（非代码文件的内容搜索）→ 不强制 codegraph；但范围限定与风险目录绕开是**全局铁律**，独立于本技能激活状态，任何 grep 调用（含日志检索）均须遵守

## ⚙️ 依赖与先决条件

- **专用搜索工具（Grep）**：支持 `path`（限定搜索目录）与 `include`（限定文件后缀/Glob 模式）参数。
- **codegraph MCP（优先但非硬依赖）**：从当前工具清单或宿主工具发现接口确认是否暴露 `codegraph_explore` 及其真实参数。其他工具（如 `codegraph_search`、`codegraph_callers`、`codegraph_files`）仅在实际暴露时使用，不假定工具族全部存在。索引与项目检查见阶段 0。
- **文件枚举工具（glob / codegraph_files）**：在目录结构不明确时，先探测 1 级目录树确认分布，再决定进入哪个子目录。
- **命令行工具（若使用 Bash rg/grep）**：类 Unix 终端环境。参数按工具区分——ripgrep 用 `-g '*.ts'` 过滤文件类型、`-g '!node_modules/**'` 排除目录；GNU grep 用 `--include='*.ts'` 过滤、`--exclude-dir=node_modules` 排除（**rg 不支持 `--exclude-dir`**，照抄会报 `unrecognized flag`）。

## 📖 标准工作流

### 阶段 0：先确认 codegraph 对当前项目是否可用

首次准备搜索当前项目时，先执行以下检查；已命中 SKIP 或用户明确指定工具时遵循对应约定，不强加探测。

1. **检查工具暴露**：先看当前已知工具清单；宿主支持延迟加载时，仅做一次针对 codegraph 的工具发现。未暴露且无法发现 → 记录“不可用”，进入阶段 1 降级，不猜测工具名、不反复探测连接。
2. **确认项目与索引**：以当前目标项目为准，按实际工具说明传入项目路径（如 `projectPath`），不能因 MCP 服务启动目录没有索引就判定所有项目不可用。优先复用已知索引状态；未知时用目录元数据检查目标项目或项目边界内的适用祖先目录是否存在 `.codegraph/`。不为探测索引越过已授权读取边界。目录存在只代表候选索引，实际可查询性由下一步确认。
3. **用首个任务查询验证**：工具存在且索引可用或状态仍未知时，按阶段 1 发起一次与任务直接相关的最小查询，同时完成可用性验证和信息获取，不额外做空查询或全仓概览。明确无索引则直接降级；调用报错、索引失效或内容明显过期时，记录具体原因并降级。能力不覆盖当前任务时直接用表中工具，无需为探活强行查询。
4. **复用状态**：在当前会话内按项目记住“可用 / 不可用及原因 / 待验证”，后续搜索复用。仅当切换项目、工具或索引状态变化、用户告知已修复，或结果与源码不符时重新检查；源码刚被修改时，对受影响文件用 Read / 窄查询核实，不能继续当作索引已同步。不自动安装、初始化、重建索引或升级 MCP。

### 阶段 1：codegraph 优先，按能力选择工具

先完成阶段 0，再按下表选择。语义、符号、调用关系探索优先 `codegraph_explore`，无需先 grep 找候选符号；仅在返回信息不足时补充其他已暴露的 codegraph 工具。以下降级搜索均须遵守阶段 2/3。

| 信息类型 | 典型问题 | 首选工具 | 降级路径 |
| :--- | :--- | :--- | :--- |
| **语义与流程** | 登录流程如何实现、AuthService 被谁调用、修改 X 影响哪些模块 | `codegraph_explore`；不足时补充已暴露的 callers / impact 工具 | 不可用 → 目录未知时先用 glob 探测，再 grep 窄查询 + Read |
| **符号定义** | getUserInfo 定义在哪个文件 | `codegraph_explore`；已知符号的精确查询可用已暴露的 search / node 工具 | 不可用或未覆盖 → grep 精确符号 + include 源码类型 |
| **字面量/报错** | 报错文案在哪行、配置常量写在哪 | 检查 codegraph 能力后，未声明支持精确文本检索时直接用 grep / `rg -F` | 不为这类查询强行走语义检索；配置符号定义按上一行处理 |
| **文件分布** | src 下有哪些 .vue 文件、模块目录结构 | 已暴露且覆盖目标目录的 `codegraph_files` | 不可用或需包含未索引文件 → glob / `rg --files`，限定目录或类型 |

用户明确要求使用 grep 等工具时，遵循用户选择并执行范围约束；不额外插入 codegraph 调用。

### 阶段 2：grep 必须限定范围（双合法模式）

grep 必须采取以下两种合法模式之一，**绝对禁止无任何过滤的全量裸搜**：

1. **模式 A · 模块级精准搜索（首选）**：
   - 目标模块明确时，显式指定具体子目录 `path`（如 `src/modules/auth`、`src/utils`）。
   - 配合 `include`（如 `*.ts`）锁定文件扩展名。
2. **模式 B · 跨模块广域搜索（备选）**：
   - 跨模块或模块未知时，`path` 允许指向源码根（`src/` 或项目对应根目录，如 uni-app 的 `pages/`），但**必须强制指定 `include`**（如 `*.{ts,vue}`），把范围钉死在目标语言，严禁不带 include 裸扫源码根。
3. **搜索词精准化（防泛词）**：
   - 使用完整标识符（如 `getUserInfo`）、带调用特征（如 `getUserInfo\(`，或直接用 `-F` 固定字符串模式免去正则转义）或词边界（如 `\bauth\b`；工具不支持 `\b` 时改用 `-w`）。
   - **禁止单字母或通用泛词**（如 `data`、`user`、`value`、`error`、`config`）进行无修饰模糊搜索。
4. **结果闭环处理机制**：
   - **结果过载（> 50 行）或超时**：立即停止，下钻子目录或追加更精确的搜索词/include，**禁止无序扩大范围或全仓扩散重试**。
   - **结果未命中（0 结果）**：先用 `glob` 或 `codegraph_files` 探测 1 级目录树确认是否猜错目录，再按模式 B（在 `src/` 下带 `include`）扩大一阶搜索，**禁止直接去除所有过滤全仓扫荡**。

### 阶段 3：绕开风险目录

以下目录**绝对禁止进入搜索范围**（除非用户显式要求）：

| 类别 | 风险目录/文件 | 潜在灾难 |
| :--- | :--- | :--- |
| **依赖安装** | `node_modules/`、`uni_modules/`、`vendor/`、`.pnpm/` | 体积巨大导致超时；vendor 副本与业务代码同名污染上下文 |
| **构建产物** | `dist/`、`build/`、`.turbo/`、`unpackage/`、`miniprogram_npm/` | 压缩代码与 SourceMap（`.min.js`/`.map`），命中即大量噪音 |
| **静态资源** | `static/`、`assets/`（含压缩包、图片、字体、大 JSON） | 二进制文件输出乱码，大文件导致搜索卡死 |
| **版本控制/IDE** | `.git/`、`.idea/`、`.vscode/` | 内部元数据噪音 |

**绕开手段**（按优先级）：
1. `path` 直达源码业务目录（如 `src/pages/`），天然隔绝风险目录；
2. `include` 限定源码扩展名（如 `*.{ts,vue,js}`），自动过滤二进制与构建产物；
3. Bash 命令行下显式追加排除参数（如 ripgrep `rg -g '!node_modules/**' -g '!uni_modules/**'`；GNU grep `--exclude-dir=node_modules`）。

### 阶段 4：执行与结果消化

1. codegraph 查询限定目标模块、符号或问题，使用实际支持的输出上限（如 `maxFiles`）；先取必要范围，不默认展开全仓调用图。阶段 3 的风险目录约束同样适用，工具无法限制时改用受限搜索。
2. codegraph 已返回的完整相关源码视为已读取，不再整文件 Read，也不再用 grep 重复确认。仅在源码缺失、截断、过期或需核实当前文件行号时，定点补读所需片段。仅有摘要或关系图不算读过源码。
3. grep 定位到文件与行号后，**使用 Read 工具读取该位置上下文**（配合 offset/limit），理解完毕即停止。codegraph 0 命中不等于源码不存在；收窄或修正查询仍无结果时，按阶段 1 补充受限搜索，避免索引覆盖不足造成漏查。
4. 维护本技能时读取 [references/evals.md](references/evals.md)，逐条核对触发与工具路由；检查描述、工作流和范例是否一致。

## ⛔ 行为护栏

- **🚫 禁止全量裸搜**：禁止既不指定具体子目录 `path`、又不指定 `include` 类型的全局扫描；禁止裸搜 `src/` 根。
- **🚫 禁止搜索风险目录**：未经用户显式要求，`node_modules/`、`uni_modules/`、`static/`、`dist/`、`build/`、`.git/` 等一律不得作为搜索路径（与阶段 3 例外条款一致）。
- **🚫 禁止跳过可用性检查**：除 SKIP 与用户指定工具外，先执行或复用阶段 0 检查；codegraph 可用且覆盖任务时必须优先，不可直接拿 grep 替代语义探索。降级必须有不可用、能力不覆盖或结果不足的具体依据。
- **🚫 禁止泛词模糊搜索**：禁止使用 `data`、`user`、`value`、`res` 等超高频泛词进行无边界模糊搜索。
- **🚫 禁止重复全量 grep**：定位到行号后立即使用 Read 工具读上下文，命中即止。
- **🛑 异常收窄与有序上浮**：结果超过 50 行或超时立即停止并收窄；0 命中时先探目录再按模式 B 上浮一阶，禁止无序全仓扩散。

## 📝 模板与范例

### 场景 1：精准搜索 vs 全量裸搜

### <Bad>
```text
# ❌ 场景：找 getUserInfo 的定义
grep pattern: "getUserInfo"              # 未指定 path，默认扫全仓
grep pattern: "getUserInfo", path: src/  # 裸搜 src/ 根，未加 include 过滤
```

### <Good>
```text
# ✅ 模式 A：下钻到具体业务子目录，限定文件类型
# 前提：已完成阶段 0，确认当前项目 codegraph 不可用；或用户明确指定 grep。
grep pattern: "getUserInfo", path: src/modules/user, include: "*.ts"

# ✅ 模式 B：跨模块位置未知时，在 src/ 下强制限定文件扩展名
grep pattern: "getUserInfo", path: src, include: "*.{ts,vue}"
# 命中后使用 Read 工具读取对应文件行号上下文
```

### 场景 2：风险目录绕开

### <Bad>
```text
# ❌ 场景：uni-app 项目，想确认 z-paging 组件用在哪
grep pattern: "z-paging", path: .  # 搜出 uni_modules 里的组件实现源码，产生大量噪音
```

### <Good>
```text
# ✅ 只在业务页面目录内搜索，排除依赖目录
# 已检查 codegraph 能力；当前为组件标签的精确文本检索。
grep pattern: "z-paging", path: src/pages, include: "*.vue"
# 若需查阅组件实现本身，直接使用 Read 读取已知路径 uni_modules/z-paging/components/...
```

### 场景 3：语义问题选择专用工具

### <Bad>
```text
# ❌ 场景：理解"登录流程是怎么实现的"
grep pattern: "login", path: src, include: "*.ts"  # 命中几十处分散调用，无法拼装完整流程
```

### <Good>
```text
# ✅ 以下为工具调用流程示意；参数以当前工具 schema 为准。
# 1. 确认已暴露 codegraph_explore，复用当前项目的索引状态。
# 2. 调用 codegraph_explore：query 为“登录流程 login auth 实现”，
#    projectPath 为已确认的当前项目绝对路径，maxFiles 先限制为 3（若支持）。
# 3. 若返回已含相关完整源码与调用关系，直接分析，不追加 grep 或 Read。
# 4. 仅在缺少上游关系且工具实际暴露时，补充 codegraph_callers。
```

### 场景 4：可用性检查只做必要次数

### <Bad>
```text
# ❌ 每次搜索都探测 MCP、读取索引目录；失败后反复调用同一个工具。
# ❌ 因 MCP 启动目录无索引而跳过另一个已建索引项目。
```

### <Good>
```text
# ✅ 首次确认项目 A 无索引 → 记录不可用原因 → 使用受限 rg / glob。
# 同一项目的后续搜索复用该结果；切换到项目 B 时独立检查其状态。
# 用户告知项目 A 已建索引后，再用实际任务查询验证，不自动执行初始化。
```

## 📜 版本变更历史

- **v1.2.0** (2026-09-18): 前置 codegraph 可用性检查，按项目复用状态；补充能力分流、故障降级、有界输出与源码去重读取规则及评测用例。
- **v1.1.1** (2026-08-20): 修复审查发现的问题——按工具区分 rg 与 GNU grep 的过滤/排除参数（实测 `rg` 不支持 `--exclude-dir`）；搜索词示例补充正则转义与 `-F`/`-w` 用法；护栏与阶段 3 的风险目录例外条款对齐（补"未经用户显式要求"）；"禁止扩大范围"改为"禁止无序扩大"，消除与 0 命中上浮的字面矛盾；声明范围限定为全局铁律（不受 SKIP 影响）；模式 B 源码根泛化（支持 `pages/` 等根目录）。
- **v1.1.0** (2026-08-20): 消除规则逻辑冲突与表述歧义：
  1. 明确双合法模式（模式 A · 模块级精准 vs 模式 B · 跨模块带 include 广域）；
  2. 补充结果闭环机制（过载 > 50 行收窄 + 0 命中时探测目录并有序上一阶上浮）；
  3. 完善泛词防御与词边界修饰约束；
  4. 隔离专用 Grep 参数与 Bash 命令行参数。
- **v1.0.0** (2026-08-17): 初始版本。规范 grep 范围限定、风险目录绕开与 codegraph 优先工具分层。
